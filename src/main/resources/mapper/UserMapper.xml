<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
    PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
    "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
    <mapper namespace="com.Lwoo.mapper.UserMapper">
    	<insert id="add" parameterType="User">
    		insert into user(username,password,sex,tel,statu,assoId) values(#{username},#{password},#{sex},#{tel},#{statu},#{assoId})
    	</insert>
    	<delete id="delete" parameterType="User">
    		delete from user where id=#{id}
    	</delete>
    	<select id="get" parameterType="_int" resultType="User">
    		select * from user where id=#{id}
    	</select>
    	<update id="update" parameterType="User">
    		update user set username=#{username},password=#{password},sex=#{sex},tel=#{tel},statu=#{statu},assoId=#{assoId} where id=#{id}
    	</update>
    	<select id="list" resultType="User">
    	select * from user 
    	</select>
    	<!-- findByUsername -->
    	<select id="findByUsername" parameterType="String" resultType="User">
    		select * from user where username=#{username}
    	</select>
    </mapper>